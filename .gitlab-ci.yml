stages:
  - build

build-job:
  stage: build
  image: node:latest
  variables:
    projectDirectory: 'quizcraft'

  script:
    - npm i
    - cd "${projectDirectory}/client" && npm install
    - npm run build $$ cd ..
  artifacts:
    paths:
      - "build/"

