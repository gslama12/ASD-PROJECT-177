stages:
  - build

build-job:
  stage: build
  image: node:latest
  variables:
    projectDirectory: 'quizcraft'

  script:
    - cd "${projectFolder}/client" && npm install && npm run build && popd
    - cd "${projectFolder}/server" && npm install && npm start && popd
  artifacts:
    paths:
      - "build/"

