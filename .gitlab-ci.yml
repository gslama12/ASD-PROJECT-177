stages:
  - build

build-job:
  stage: build
  image: node:latest
  variables:
    projectDirectory: 'quizcraft'

  script:
    - echo npm -v
    - npm i
    - cd "${projectDirectory}/client" && npm i && cd ..
    - cd "client" && npm run dev && cd ..
  artifacts:
    paths:
      - "build/"

