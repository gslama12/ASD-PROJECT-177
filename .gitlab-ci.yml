stages:
  - build

build-job:
  stage: build
  image: node:latest
  variables:
    projectDirectory: 'quizcraft'

  script:
    - npm i
    - cd "${projectDirectory}/client" && npm i && cd ..
    - cd "${projectDirectory}/client" && npm run dev && cd ..
  artifacts:
    paths:
      - "build/"

